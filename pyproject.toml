[project]
name = "sigoper-companion-service"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "beautifulsoup4>=4.15.0",
  "fastapi[standard-no-fastapi-cloud-cli]>=0.141.1",
  "httpx>=0.28.1",
  "websockets>=17.1",
]

[dependency-groups]
dev = ["pytest>=9.1.1"]

[tool.pytest.ini_options]
pythonpath = ["."]
